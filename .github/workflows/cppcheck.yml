name: cppcheck
on: [push]

jobs:
  build:
    name: cppcheck-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          packages: cppcheck
      - name: doxygen
        run: |
          doxygen ${{ github.workspace }}
